# Ensure native binaries are installed for the current platform
# This fixes the npm optional dependencies bug (https://github.com/npm/cli/issues/4828)
# where rollup's platform-specific binaries may not resolve on all OS.
supportedArchitectures["os"]="current"
supportedArchitectures["cpu"]="current"
supportedArchitectures["libc"]="current"
